@model int?
@{ 
    int _TmpData = 0;
    if (Model.HasValue) {
        _TmpData = Model.Value;
    }
    var data = new MVCHomeWork.Controllers.TemplateDataController().GetCustCartTypeName(_TmpData);
    @Html.Encode(data);
}
