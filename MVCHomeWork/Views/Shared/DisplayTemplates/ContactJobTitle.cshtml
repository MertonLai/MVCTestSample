@model string
@{ 
    string _TmpData = "";
    if (!string.IsNullOrWhiteSpace(Model)) {
        _TmpData = Model;
    }
    var data = new MVCHomeWork.Controllers.TemplateDataController().GetJobTitleName(_TmpData);
    @Html.Encode(data);
}
