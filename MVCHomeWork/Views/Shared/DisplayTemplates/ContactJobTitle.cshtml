@model string
@{ 
    string _TmpData = "";
    if (!string.IsNullOrWhiteSpace(Model)) {
        _TmpData = Model;
    }
    var data = new MVCHomeWork.BLL.SysUtility().GetJobTitleName(_TmpData);
    @Html.Encode(data);
}
